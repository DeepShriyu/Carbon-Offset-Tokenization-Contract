[project]
name = 'carbon-offset-tokenization'
description = ''
authors = []
telemetry = true
cache_dir = './.cache'
requirements = []
[contracts.peer-to-peer-car-rental-contract]
path = 'contracts/peer-to-peer-carbon-offset-tokenization.clar'
lar
epoch = 3.1
[repl.analysis]
passes = ['check_checker']

[repl.analysis.check_checker]
strict = false
trusted_sender = false
trusted_caller = false
callee_filter = false

[repl.remote_data]
enabled = false
api_url = 'https://api.hiro.so'
